/** Canonical production origin — single source of truth for SEO URLs. */
export const SITE_ORIGIN = 'https://iobjectm.com'

export const SITE_NAME = 'IOM — Interactive Object Media'

export const SITE_SHORT_NAME = 'IOM'

export const DEFAULT_TITLE = 'IOM — Interactive Object Media'

export const DEFAULT_DESCRIPTION =
  'IOM provides senior realtime 3D specialist support, external production capacity and complete interactive projects using browser-based 3D, Blender, Unreal, CAD/BIM and spatial media.'

export const DEFAULT_OG_IMAGE = `${SITE_ORIGIN}/og-image.svg`

export const CONTACT_EMAIL = 'contact@iobjectm.com'

/** Social / entity profiles for Organization schema (extend as profiles are added). */
export const SAME_AS: string[] = [
  'https://3dbviewer.com/',
]

export const DEFAULT_KEYWORDS = [
  'interactive object media',
  '3D viewer',
  '360 panorama tour',
  'WebGPU',
  'Three.js',
  'immersive web',
  'real-time 3D',
  'virtual tour editor',
  'interactive media studio',
]
