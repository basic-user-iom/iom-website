# Custom cursor — labelled v1 (parked)

Snapshot of the first IOM custom cursor: precision tip + inertial ring with
labelled modes (`VIEW`, `PLAY`, `LOOK`, `EXPLORE`, `ENTER 3D`, `START`, `DRAG`, …).

## Live demo

https://iobjectm.com/demos/custom-cursor-labelled/

Local: `/demos/custom-cursor-labelled/`

Playground + usage code side by side. Source for the static demo lives in
`public/demos/custom-cursor-labelled/`.

## Snapshot

This folder keeps the original React module snapshot from 2026-08-06
(`CustomCursor`, `mountCustomCursor`, labelled CSS, etc.).

The live homepage now uses a quieter `data-cursor="focus"` cyan orb on cards;
labelled modes remain available for CTAs and this experiment.
